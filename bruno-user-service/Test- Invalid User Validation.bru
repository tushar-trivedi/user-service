meta {
  name: Test: Invalid User Validation
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/api/v1/auth/validate-user
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "nonexistent@example.com",
    "password": "wrong_password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
